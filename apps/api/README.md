PR Manager API

FastAPI service for webhook intake, deterministic PR rules, AI review, and
analytics. Git hosts are adapters under `app/scm/` (Gitea, GitHub, GitLab).
Merge authority always remains with humans.
