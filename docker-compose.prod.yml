services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-prmanager}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prmanager}
      POSTGRES_DB: ${POSTGRES_DB:-prmanager}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-prmanager} -d ${POSTGRES_DB:-prmanager}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  api:
    image: pr-mangement-api:latest
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-prmanager}:${POSTGRES_PASSWORD:-prmanager}@postgres:5432/${POSTGRES_DB:-prmanager}
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      APP_ENV: production
    ports:
      - "127.0.0.1:8110:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s

  dashboard:
    image: pr-mangement-dashboard:latest
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "8111:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres-data:
